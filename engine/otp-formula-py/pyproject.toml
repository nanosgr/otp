[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "otp-engine"
requires-python = ">=3.9"
description = "Motor de fórmulas de online-otp (Rust)"
dynamic = ["version"]

[tool.maturin]
module-name = "otp_engine"
